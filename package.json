{
  "name": "svgx",
  "version": "0.7.2",
  "description": "is a universal SVG renderer",
  "main": "svgx.js",
  "engines": {
    "node": ">=4"
  },
  "scripts": {
    "check-deps": "npm outdated",
    "example": "NODE_PATH=. babel-node example.js",
    "gh-pages:_data": "for x in package.json tags.json; do npm run gh-pages:_data:$x; done",
    "gh-pages:_data:package.json": "cp package.json gh-pages/_data/",
    "gh-pages:_data:tags.json": "wget https://api.github.com/repos/fibo/SVGx/tags -O gh-pages/_data/tags.json",
    "gh-pages:push": "git subtree --prefix gh-pages push origin gh-pages",
    "gh-pages:pull": "git subtree --prefix gh-pages pull origin gh-pages",
    "homepage": "echo \"---\ntitle: $npm_package_name\n---\" > gh-pages/index.md; cat README.md >> gh-pages/index.md",
    "jekyll": "npm run homepage; npm run gh-pages:_data; cd gh-pages; jekyll build; cd ..",
    "lint": "standard svgx.js test.js",
    "postversion": "git push origin v${npm_package_version}; npm publish; npm run gh-pages:_data",
    "postgh-pages:_data": "git commit -am ':arrow_up: updated version'; npm run push",
    "push": "git push origin master; npm run gh-pages:push",
    "test": "NODE_PATH=. tape -r babel-register test.js"
  },
  "pre-commit": [
    "lint",
    "test",
    "check-deps"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fibo/SVGx.git"
  },
  "keywords": [
    "svg",
    "universal",
    "react"
  ],
  "author": {
    "name": "Gianluca Casati",
    "url": "http://g14n.info"
  },
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/fibo/SVGx/issues"
  },
  "homepage": "http://g14n.info/SVGx",
  "devDependencies": {
    "babel-cli": "6.x",
    "babel-preset-es2015": "6.x",
    "babel-preset-react": "6.x",
    "pre-commit": "1.x",
    "react": "15.x",
    "react-dom": "15.x",
    "standard": "9.x",
    "tape": "4.x"
  },
  "dependencies": {
    "cheerio": "^0.22.0"
  }
}
